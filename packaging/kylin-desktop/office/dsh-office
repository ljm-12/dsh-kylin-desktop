#!/usr/bin/env bash
set -euo pipefail
OFFICE_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
export PYTHONNOUSERSITE=1
export PYTHONPATH="$OFFICE_DIR/python/lib/python3.10/site-packages:${PYTHONPATH:-}"
exec "$OFFICE_DIR/python/bin/python3" -I "$OFFICE_DIR/office_tool.py" "$@"
